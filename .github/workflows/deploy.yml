name: Deploy Run (run.movmais.com) - COM DEBUG

on:
  push:
    branches:
      - main
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "📍 Conectar ao servidor"
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            echo "=========================================="
            echo "🚀 INICIANDO DEPLOY DO RUN"
            echo "=========================================="
            echo ""

            echo "📍 Data/Hora do deploy"
            date
            echo ""

            echo "📍 Verificar diretório"
            pwd
            ls -la /opt/movmais/apps/
            echo ""

            echo "📍 Versão atual no servidor (ANTES do deploy)"
            cd /opt/movmais/apps/run
            echo "Commit atual:"
            git log -1 --oneline
            echo "Branch:"
            git branch
            echo ""

            echo "📍 Atualizar código do GitHub"
            git fetch origin
            echo "Commits disponíveis:"
            git log -3 --oneline
            echo ""

            echo "📍 Fazer reset hard para origin/main"
            git reset --hard origin/main
            echo "✅ Reset concluído"
            echo ""

            echo "📍 Versão APÓS git reset"
            git log -1 --oneline
            echo ""

            echo "📍 Verificar arquivos alterados"
            git status
            echo ""

            echo "📍 Verificar conteúdo de arquivo importante (package.json)"
            head -5 package.json
            echo ""

            echo "=========================================="
            echo "📝 CRIAR ARQUIVO .ENV COM SECRETS"
            echo "=========================================="
            cat > /opt/movmais/apps/run/.env << 'ENVEOF'
            DB_HOST=${{ secrets.DB_HOST }}
            DB_PORT=${{ secrets.DB_PORT }}
            DB_DATABASE=${{ secrets.DB_DATABASE }}
            DB_USERNAME=${{ secrets.DB_USERNAME }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            PORT=${{ secrets.PORT }}
            NODE_ENV=production
            ENVEOF
            echo "✅ Arquivo .env criado com secrets do GitHub"
            echo ""

            echo "=========================================="
            echo "🐳 DOCKER BUILD"
            echo "=========================================="
            cd /opt/movmais
            echo ""

            echo "📍 Remover imagem antiga"
            docker rmi movmais-run -f || echo "Nenhuma imagem anterior"
            echo ""

            echo "📍 Parar container antigo"
            docker-compose stop run || echo "Container não estava rodando"
            echo ""

            echo "📍 Iniciar build (sem cache)"
            docker-compose build --no-cache run
            BUILD_STATUS=$?

            if [ $BUILD_STATUS -eq 0 ]; then
              echo "✅ BUILD SUCESSO"
            else
              echo "❌ BUILD FALHOU"
              exit 1
            fi
            echo ""

            echo "=========================================="
            echo "🚀 INICIAR CONTAINER"
            echo "=========================================="
            docker-compose up -d run
            echo ""

            echo "📍 Aguardar 5 segundos para container iniciar"
            sleep 5
            echo ""

            echo "📍 Status dos containers"
            docker-compose ps
            echo ""

            echo "📍 Logs do container (últimas 30 linhas)"
            docker logs movmais-run | tail -30
            echo ""

            echo "=========================================="
            echo "✅ VERIFICAÇÕES FINAIS"
            echo "=========================================="

            echo "📍 Verificar versão no servidor (APÓS deploy)"
            cd /opt/movmais/apps/run
            echo "Commit atual:"
            git log -1 --oneline
            echo ""

            echo "📍 Verificar se container está rodando"
            docker-compose ps run
            echo ""

            echo "📍 Testar acesso ao Run"
            curl -s -I http://127.0.0.1:3030/ | head -3
            echo ""

            echo "📍 Verificar porta 3030 está aberta"
            netstat -tlnp | grep 3030 || echo "⚠️ Porta 3030 não encontrada"
            echo ""

            echo "=========================================="
            echo "✅ DEPLOY CONCLUÍDO COM SUCESSO!"
            echo "=========================================="
            echo "Timestamp: $(date)"
            echo "URL: https://run.movmais.com"
            echo "=========================================="
